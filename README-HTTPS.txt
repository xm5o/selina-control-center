SELINA CONTROL CENTER v1.1 FRONTEND

This frontend now uses polling rather than SSE so it works through a
Cloudflare Quick Tunnel.

Do not publish this to GitHub Pages yet unless the HTTPS backend test succeeds.
